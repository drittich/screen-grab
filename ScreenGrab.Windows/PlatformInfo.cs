namespace ScreenGrab.Windows;

// Placeholder. Phase 4+ move the existing user32/gdi32 P/Invoke, GDI capture,
// RegisterHotKey, StartupManager (registry) and Mutex single-instance here.
public static class PlatformInfo
{
	public const string Name = "ScreenGrab.Windows";
}
